let arr = [1,2,3,4,5,6,7]

// function for rotating the array
const arrayRotationSimple = (swapValue) =>{

    let tempArr = []

    for(let i = 0; i<swapValue; i++){
        tempArr[i] = arr[i]
    }

}